
// Заданная строка
let str = '1-2-3-4-5';

// Замена всех дефисов на точки
let modifiedStr = str.replace(/-/g, '.');
console.log(`Исходная строка: ${str}`);
console.log(`Модифицированная строка: ${modifiedStr}`);

